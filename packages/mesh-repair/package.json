{
  "name": "@cadfixer/mesh-repair",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "description": "Conservative deterministic mesh repair. Exact-topology operations only: no tolerance, no reconstruction, no geometry kernel. Produces validated candidates; never mutates authoritative geometry.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@cadfixer/mesh-core": "*",
    "@cadfixer/mesh-topology": "*",
    "@cadfixer/shared": "*"
  }
}
