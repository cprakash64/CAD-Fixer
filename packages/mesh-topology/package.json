{
  "name": "@cadfixer/mesh-topology",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "description": "Read-only topological analysis of a canonical mesh. Recovers connectivity from exact stored coordinates; never modifies geometry.",
  "//exports": "./fixtures exposes the deterministic topology fixtures so mesh-repair's tests exercise the SAME geometry the topology engine is pinned against, instead of growing a second, divergent copy. Test-only: no production path imports it, and the bundle scan checks that.",
  "exports": {
    ".": "./src/index.ts",
    "./fixtures": "./src/fixtures.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@cadfixer/mesh-core": "*",
    "@cadfixer/shared": "*"
  }
}
